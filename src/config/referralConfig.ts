/**
 * Feature flag for the Referral Program UI.
 * Set to `false` to hide referral entry points for initial launch.
 * Set to `true` to resurface all referral pages, cards, and menu items.
 */
export const SHOW_REFERRAL_PROGRAM = false;
